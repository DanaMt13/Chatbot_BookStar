# Copiază acest fișier în .env și completează valorile local (nu-l comite!)
OPENAI_API_KEY=REPLACE_ME
CHAT_MODEL=gpt-4o-mini
EMBED_MODEL=text-embedding-3-small
PERSIST_DIR=/app/chroma_store

# TTS (opțional)
TTS_MODE=openai
TTS_VOICE=alloy
TTS_FORMAT=mp3
TTS_RATE=170
TTS_VOLUME=0.8

MODERATION_ENABLED=1
